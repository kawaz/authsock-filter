skip_output:
  - meta           # lefthook version banner
  - empty_summary  # summary when nothing to run
  - success        # successful step output

pre-push:
  commands:
    fmt-check:
      run: cargo fmt --all -- --check
    clippy:
      run: cargo clippy --all-targets --all-features -- -D warnings
